<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_container"
    android:layout_width="300dp"
    android:layout_height="300dp"
    android:layout_gravity="center"
    android:padding="8dp">

    <!-- Main card background with dot matrix -->
    <LinearLayout
        android:id="@+id/dot_matrix_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/dot_matrix_bg"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Dot matrix rendered as bitmap -->
        <ImageView
            android:id="@+id/dot_matrix_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Battery dot matrix"
            android:scaleType="fitCenter"
            tools:ignore="ContentDescription" />

    </LinearLayout>

</FrameLayout>
